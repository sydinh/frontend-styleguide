import React from 'react';
import { Helmet } from 'react-helmet';

const CSS = () => (
  <section>
    <Helmet>
      <title>Styleguide - CSS</title>
      <meta name="description" content="Styleguide - CSS" />
      <meta name="theme-color" content="#000000" />
    </Helmet>
    <h2>CSS</h2>
  </section>
);

export default CSS;
